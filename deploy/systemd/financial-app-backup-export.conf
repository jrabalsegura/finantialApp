# Drop-in: /etc/systemd/system/financial-app-backup.service.d/export.conf
# Deja una copia de cada backup legible por el usuario que la baja al Mac.
[Service]
Environment=FINANCIAL_APP_BACKUP_EXPORT_DIR=/home/jrabal/financial-app-backups
Environment=FINANCIAL_APP_BACKUP_EXPORT_OWNER=jrabal
